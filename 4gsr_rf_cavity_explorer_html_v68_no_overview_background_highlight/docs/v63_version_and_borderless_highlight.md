# v63 Version / Borderless Highlight Fix

## Scope
Only footer version display and visual highlight style were changed.

## Fix
- Footer visible version now shows `V.63` instead of old `V.53`.
- WOO signature is preserved.
- Scenario/Input highlight no longer uses outline/border lines.
- Calculated Results highlight uses only soft fill color.
- No formula or calculation logic changed.
