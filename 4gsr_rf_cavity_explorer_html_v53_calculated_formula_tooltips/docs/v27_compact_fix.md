# v27 Compact Fix

## User-requested fixes

1. KPI widgets
- Restored moderate number size.
- Units stay inline after values without overlap.
- Content centered vertically and horizontally.

2. Bottom summary widgets
- Centered value/unit layout.
- Prevented overflow outside widgets.

3. Parameter setting groups
- Removed internal scroll inside each parameter group.
- Collapsed group height minimized.
- Expanded group uses natural height.
- Sidebar itself can scroll only when the screen height is insufficient.

4. Footer version
- Changed visible version format from `Version V25` to compact `V.27`.
